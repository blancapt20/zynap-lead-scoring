# Copy this file to .env and fill values for your local setup.
# Keep API_KEY empty to run in deterministic mock mode.
API_KEY=

# Required only when API_KEY is set (OpenAI mode).
LLM_MODEL=gpt-4o-mini

# Optional generation controls (OpenAI mode).
LLM_TEMPERATURE=0.0
LLM_TOP_P=1.0
